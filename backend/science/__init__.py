"""Deterministic scientific core of the gamma attenuation application.

Nothing in this package depends on the web layer. Every numerical result is
computed from the referenced datasets in ``backend/data``.
"""
